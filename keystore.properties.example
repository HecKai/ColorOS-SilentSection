# 用法：复制为 keystore.properties（该文件已在 .gitignore 中），填入自己的签名信息。
# 不创建也能构建：Gradle 会退回使用 debug 签名（只是换签名后需要先卸载已装版本）。
storeFile=oplus_silent_section.jks
storePassword=changeit
keyAlias=oplusmod
keyPassword=changeit

