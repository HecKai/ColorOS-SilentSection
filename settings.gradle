pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "ColorOSEnhance"
include ':app'
include ':xposedstub'      // Xposed API 的最小桩（仅编译期使用，运行期由 LSPosed 提供）
